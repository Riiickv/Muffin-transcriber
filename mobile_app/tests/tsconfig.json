{
  "//": "Tests run under Node (node --test), not React Native, so they need Node types and .ts-extension imports. Kept separate so the app's typecheck stays RN-only.",
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "types": ["node"],
    "module": "nodenext",
    "moduleResolution": "nodenext",
    "allowImportingTsExtensions": true,
    "noEmit": true
  },
  "//include": "just the test files; tsc follows their imports into the pure utils/ modules under test",
  "include": ["**/*.ts"]
}
